  <header id="header" class="header d-flex align-items-center fixed-top">
    <div class="container-fluid container-xl d-flex align-items-center justify-content-between">

      <a href="{{ path('index_route') }}" class="logo d-flex align-items-center">
        <!-- Uncomment the line below if you also wish to use an image logo -->
        <!-- <img src="assets/img/logo.png" alt=""> -->
        <h1>Ambiance voyage</h1>
      </a>

      <i class="mobile-nav-toggle mobile-nav-show bi bi-list"></i>
      <i class="mobile-nav-toggle mobile-nav-hide d-none bi bi-x"></i>
      <nav id="navbar" class="navbar">
        <ul>
          <li><a href="{{ path('index_route') }}" class="active">Page d'accueil</a></li>
          <li><a href="{{ path('rooms_list') }}" class="active">Hotels</a></li> 
          <li><a href="{{ path('events_route') }}" class="active">Événements</a></li>
          <li><a href="{{ path('bus_reservation_route') }}" class="active">Réservation de bus</a></li> 
          <li><a href="{{ path('our_trips_route',{type:2}) }}" class="active">Vols</a></li>
          <li><a href="{{ path('our_trips_route',{type:1}) }}" class="active">Voyage en bateau</a></li>
          
          
          



          
          
          {% if app.user != null %}
            <li><a href="#" class="active">{{ app.user.email }}</a></li>

            <li><a href="/logout" class="active text-danger">Se déconnecter</a></li>
            


          {% else %}
            <li><a href="{{ path('app_login') }}" class="active">Mon compte</a></li>

          {% endif %}



        </ul>
      </nav><!-- .navbar -->

    </div>
  </header>